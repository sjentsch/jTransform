---
name:  sort
title: Sort datasets
menuGroup: Data
menuSubgroup: jTransform
version: '1.0.0'
jas: '1.2'

description:
    main: Sort data set
    R:
        dontrun: true
        usage: |
            # the function is a wrapper for jmvReadWrite::sort_omv
            # please use that function when in R (or in Rj)
            # for more information: https://sjentsch.github.io/jmvReadWrite

completeWhenFilled: true

options:
    - name: data
      type: Data
#      description:
#          R: the data as a data frame

    - name: varSrt
      title: Sort Index Variable(s)
      type: Variables
      permitted:
        - numeric
        - factor
        - id
#      description:
#          ui: the variables to sort `data` with
#          R: a vector of strings naming the variables to sort `data` with

    - name: varAll
      type: Variables
      hidden: true
      permitted:
        - numeric
        - factor
        - id

    - name: ordSrt
      type: Array
      items: (varSrt)
      default: null
      template:
        type: Group
        elements:
          - name: var
            type: Variable
            content: $key
          - name: order
            type: List
            options:
              - name: ascend
                title: ascending
              - name: descend
                title: descending
#      description:
#          ui: >
#            the order with which variables are sorted
#          R: >
#            a list of lists specifying the order with which variables are
#            sorted, either `'ascending'`, or `'descending'`

    - name: btnOut
      type: Action
      title: CREATE


    - name: varIncl
      title: Variables to include
      type: Variables
      permitted:
        - numeric
        - factor
        - id
#      description:
#          ui: the variables to include in the new dataset
#          R: the variables to include in the new data.frame

    - name: varExcl
      title: Variables to exclude
      type: Variables
      permitted:
        - numeric
        - factor
        - id
#      description:
#          ui: the variables to include in the new dataset
#          R: the variables to include in the new data.frame

...
